version: "3.8"

services:
  hdr-ui:
    image: node:18-alpine
    container_name: hdr-ui
    command: sleep infinity
    ports:
      - "3000:3000"
    volumes:
      - ./:/app/
    working_dir: /app/